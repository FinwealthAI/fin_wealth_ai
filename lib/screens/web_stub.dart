// Stub file for non-web platforms
// These functions are only used on web, so we provide empty implementations

void registerIframeViewFactory(String viewId, String url) {
  // No-op on non-web platforms
}
